package api

import (
	"net/http"

	"github.com/anabiozz/goods/backend/common"
)

// GetGraphicsHandler ..
func GetGraphicsHandler(env *common.Env) http.HandlerFunc {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		
	})
}
